function drawIntroScreen() {	
	clear()
		background(backgroundImage)
	textAlign(CENTER)
	textSize(50)
	fill('blue')
	text("Welcome to evoliton", windowWidth / 2, windowHeight / 2)
	text("Press <ENTER> to play", windowWidth / 2, windowHeight / 2 + 100)
}